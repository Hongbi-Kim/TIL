## 연습문제

#### 1. 평균 구하기

- 답1)

  ```python
  import numpy as np
  a = [80,75,55]
  np.mean(a)
  ```

- 답2)

  ```python
  a = 80
  b = 75
  c = 55
  (a+b+c)/3
  #70.0
  ```

  

####  2. 홀짝 구하기
```python
x = 13
if x%2==0:
 print("짝수")
else:
 print("홀수")
```



#### 3. 주민등록번호를 연월일(YYYYMMDD) 부분과 그 뒤의 숫자 부분으로 나누어 출력.    
```python
pin = "881120-1068234"
yyyymmdd = pin[:6]
num = pin[7:]
print(yyyymmdd) #881120
print(num) #1068234
```



#### 4. 인덱싱
```python
pin = "881120-1068234"
pin[7]
#1
```



#### 5.replace
```python
a = "a:b:c:d"
a.replace(":", "#")
```



#### 6. 내림차순 - 내장 함수
```python
a = [1,3,5,4,2]
a.sort(reverse=True)
a
#[5,4,3,2,1]
```



#### 7. 리스트를 문자열로 출력
````python
a = ['Life','is','too','short']
result = " ".join(a)
print(result)
#Life is too short
````



#### 8.튜플에 값 추가
```python
a = (1,2,3)
b = (4,)
a + b
#(1,2,3,4)
```



#### 9.딕셔너리
```python
a = dict()
a
a['name'] = 'python'
a[('a',)] = 'python'
a[[1]] = 'python' #unhashable type: 'list'
a[250] = 'python'
```



#### 10. 딕셔너리 a에서 'B'에 해당되는 값 추출
```python
a = {'A':90,'B':80,'C':70}
a.pop('B')
a
#{'A': 90, 'C': 70}
```



#### 11. a리스트에서 중복 숫자 제거
```python
a = [1,1,1,2,2,3,3,3,4,4,5]
set(a) 
#{1, 2, 3, 4, 5}

list(set(a)) 
#[1, 2, 3, 4, 5]
```



#### 12. 이유 설명(a 변경 시 b 변경되는 이유)
```python
a = b = [1,2,3]
a[1] = 4
print(b)
#a와 b 변수는 모두 동일한 리스트 객체 가르킴
```

